# README images

Drop the following file into this folder so the top of the repo `README.md` renders correctly:

| File | Source |
|---|---|
| `fabric-logo.svg` | Microsoft Fabric brand assets — https://learn.microsoft.com/fabric/ (download the official logo SVG) |

The file is referenced from [`/README.md`](../../README.md) via a relative path. Until it is added the image will render as a broken-image icon; the rest of the README is unaffected.

> Use Microsoft's official, current brand assets only — don't ship modified or third-party recreations.
