# README images

Drop the following two files into this folder so the top of the repo `README.md` renders correctly:

| File | Source |
|---|---|
| `fabric-logo.svg` | Microsoft Fabric brand assets — https://learn.microsoft.com/fabric/ (download the official logo SVG) |
| `fabric-data-agents.png` | Screenshot of the Fabric Data Agents experience, or the official product banner from Microsoft Learn |

Both files are referenced from [`/README.md`](../../README.md) via relative paths. Until they are added the images will render as broken-image icons; the rest of the README is unaffected.

> Use Microsoft's official, current brand assets only — don't ship modified or third-party recreations.
